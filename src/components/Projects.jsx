const Projects = () => {
	return (
		<section id="projects" className="section is-medium">
			<h1 className="title">Projects</h1>
			<p className="has-text-centered">
				Pull from GitHub or do it manually so the asset packs can be
				here too
			</p>
		</section>
	);
};

export default Projects;
